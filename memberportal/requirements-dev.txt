# Test/development dependencies.
# Install alongside the runtime deps:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest~=8.4
pytest-django~=4.14
pytest-asyncio~=0.23.8
pytest-cov~=7.1
# Sets the MM_* env vars before pytest-django calls django.setup(); see pytest.ini.
pytest-env~=1.1.5
